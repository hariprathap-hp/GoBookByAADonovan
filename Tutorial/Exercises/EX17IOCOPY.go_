package main

import (
	//"fmt"

	"fmt"
	"io"
	"log"
	"net/http"
	"os"
)

func main() {
	url := "https://www.york.ac.uk/teaching/cws/wws/webpage1.html"
	resp, err := http.Get(url)
	if err != nil {
		log.Fatalf("Getting url failed with error %v\n", err)
	}

	defer resp.Body.Close()
	res, c_err := io.Copy(os.Stdout, resp.Body)
	if c_err != nil {
		fmt.Println(c_err)
	}

	fmt.Println(res)

}
